# E2E 匿名JWT→AI→APP（SSE）闭环测试配置（示例）
# 注意：请复制为 .env.local 并填入真实值；不要提交 .env.local

# Supabase 配置
SUPABASE_URL=https://<your-project>.supabase.co
SUPABASE_ANON_KEY=<your-anon-key>

# 可选：每日真实用户 E2E（创建用户→登录→SSE）需要 Service Role Key（不要提交 .env.local）
SUPABASE_SERVICE_ROLE_KEY=<your-service-role-key>

# 后端 API 配置
API_BASE=http://localhost:9999

# 可选：Mail API（真实邮箱流测试）
MAIL_API_BASE_URL=https://taxbattle.xyz
MAIL_API_KEY=<your-mail-api-key>
MAIL_DOMAIN=moemail.app

# 可选：E2E 邮箱模式（默认 auto：有 MAIL_API_KEY 则使用 mailapi）
# E2E_EMAIL_MODE=auto
# 可选：local 模式邮箱域名（如果 Supabase 开了邮箱域名白名单）
# E2E_LOCAL_EMAIL_DOMAIN=moemail.app
# MAIL_API_MAX_WAIT_SECONDS=60
# MAIL_API_POLL_INTERVAL_SECONDS=2
# MAIL_EXPIRY_MS=3600000
