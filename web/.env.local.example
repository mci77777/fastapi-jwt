# Web 本地配置示例（不会被提交：你应复制为 `web/.env.local` 或运行同步脚本生成）
#
# 推荐：从 E2E 的 SSOT 同步生成（不会打印密钥，不入库）
#   python3 scripts/dev/sync_e2e_env_to_web.py
#
# 读取：e2e/anon_jwt_sse/.env.local
# 写入：web/.env.development.local（已被 gitignore 忽略）

VITE_SUPABASE_URL=https://<your-project>.supabase.co
VITE_SUPABASE_ANON_KEY=<your-anon-key>

# 可选：用于 UI 展示/代理覆盖
VITE_API_URL=http://localhost:9999
VITE_PROXY_TARGET=http://localhost:9999

